# VitaProd Bot 🍓

AI-ассистент для оптовой компании замороженных продуктов. Telegram бот с RAG-архитектурой для ответов на вопросы о ценах и наличии товаров.

## Возможности

### Текущие (MVP)
- ✅ Ответы на вопросы о ценах товаров
- ✅ Проверка наличия товаров
- ✅ Семантический поиск по каталогу
- ✅ Эскалация на менеджера при неуверенности

### Планируемые
- 📋 История цен и сравнение ("черника подорожала?")
- 🔔 Уведомления о поступлении товаров
- 👤 Профили клиентов и персонализация
- 📊 Рекомендации на основе истории покупок
- 🔗 Интеграция с 1C

## Технологии

| Компонент | Технология |
|-----------|------------|
| Telegram Bot | aiogram 3.x |
| Backend | FastAPI |
| SQL Database | SQLite → PostgreSQL |
| Vector Database | Qdrant |
| LLM | GigaChat |
| Embeddings | sentence-transformers |

## Быстрый старт

### 1. Клонирование и настройка

```bash
git clone https://github.com/kerz-A/vitaprod_bot
cd vitaprod_bot

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Конфигурация

```bash
# Копируем пример конфигурации
cp .env.example .env

# Редактируем .env, заполняем:
# - TELEGRAM_BOT_TOKEN (получить у @BotFather)
# - GIGACHAT_CREDENTIALS (получить на developers.sber.ru)
```

### 3. Запуск Qdrant

```bash
docker-compose up -d qdrant
```

### 4. Инициализация БД и загрузка прайса

```bash
# Инициализация таблиц
python scripts/init_db.py

# Загрузка прайс-листа
python scripts/load_price.py data/prices/price_list.pdf
```

### 5. Запуск бота

```bash
python -m src.main
```

## Структура проекта

```
vitaprod_bot/
├── src/
│   ├── bot/              # Telegram бот
│   │   ├── handlers/     # Обработчики сообщений
│   │   └── middlewares/  # Middleware (авторизация, логирование)
│   ├── core/             # Бизнес-логика
│   │   ├── rag/          # RAG pipeline (retriever + generator)
│   │   ├── prices/       # [TODO] Работа с ценами
│   │   └── customers/    # [TODO] Работа с клиентами
│   ├── data/             # Работа с данными
│   │   ├── parsers/      # Парсеры PDF/XLSX
│   │   └── loaders/      # Загрузчики данных
│   ├── db/               # Базы данных
│   │   ├── models.py     # SQLAlchemy модели
│   │   ├── sqlite.py     # SQLite подключение
│   │   └── vector.py     # Qdrant клиент
│   └── integrations/     # Внешние сервисы
│       └── llm/          # LLM провайдеры
├── data/                 # Данные (не в git)
├── scripts/              # Утилиты
└── tests/                # Тесты
```

## Получение токенов

### Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

### GigaChat Credentials

1. Зарегистрируйтесь на [developers.sber.ru](https://developers.sber.ru)
2. Создайте проект и получите credentials
3. Скопируйте в `.env` (формат: base64 от `client_id:client_secret`)

## Работа с прайс-листами

### Поддерживаемые форматы
- PDF (структура как в примере)
- XLSX/XLS

### Загрузка нового прайса

```bash
python scripts/load_price.py path/to/new_price.pdf
```

### Добавление причин изменения цен

Создайте файл `data/price_reasons.json`:

```json
{
    "2025-12-15": {
        "Черника": "сезонное повышение закупочных цен",
        "Клубника": "новый урожай, снижение цены"
    }
}
```

Загрузите с причинами:

```bash
python scripts/load_price.py price.pdf --reasons data/price_reasons.json
```

## Разработка

### Запуск тестов

```bash
pytest tests/
```

### Форматирование кода

```bash
black src/
ruff check src/ --fix
```

