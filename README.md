# VitaProd Bot 🍓

AI-ассистент для оптовой компании замороженных и сушёных продуктов. Telegram бот с RAG-архитектурой, памятью диалогов, голосовыми сообщениями и системой оформления заказов.

## Возможности

### Реализовано ✅
- ✅ Консультант **Себастьян Перейра** — дружелюбный AI-ассистент
- ✅ Ответы на вопросы о ценах и наличии товаров
- ✅ Семантический поиск по каталогу (138 товаров в 10 категориях)
- ✅ **Память диалогов** — бот помнит контекст разговора (LangGraph + PostgreSQL)
- ✅ **Голосовые сообщения** — распознавание речи (Groq Whisper)
- ✅ **Система заказов** с пошаговым оформлением:
  - Распознавание намерения заказа из диалога
  - Inline-кнопки для управления заказом
  - Выбор упаковки (коробка/мешок) и веса тарного места
  - Валидация телефона, даты, времени доставки
  - Предупреждение о выходных днях
  - Выбор доставки или самовывоза
  - Экспорт заказа в XLSX
  - Отправка заявки менеджерам в Telegram
- ✅ Уведомления нескольким менеджерам
- ✅ Мониторинг работоспособности бота
- ✅ Эскалация на менеджера при необходимости

### Планируемые 📋
- 📋 Автозаполнение данных для повторных клиентов
- 📋 История заказов клиента
- 📋 Уведомления о статусе заказа
- 📊 Аналитика и отчёты
- 🔗 Интеграция с 1C

## Категории товаров

| Категория | Форма | Примеры |
|-----------|-------|---------|
| Ягоды | Замороженные | Черника, малина, клубника, смородина |
| Овощи | Замороженные | Перец, баклажан, горошек, кукуруза |
| Фрукты | Замороженные | Вишня, абрикос, ананас, гранат |
| Грибы | Замороженные/Сушёные | Белый гриб, лисички, маслята |
| Орехи | Замороженные | Каштан, кедровый орех |
| Плоды | Сушёные | Боярышник, калина, шиповник |
| Травы | Сушёные | Лист смородины |
| Компотные смеси | Замороженные | 10 видов смесей |
| Овощные смеси | Замороженные | Европейская, гавайская и др. |

## Технологии

| Компонент | Технология |
|-----------|------------|
| Telegram Bot | aiogram 3.x + FSM |
| Оркестрация диалогов | LangGraph |
| Память диалогов | PostgreSQL (AsyncPostgresSaver) |
| Vector Database | Qdrant |
| LLM | GigaChat |
| Speech-to-Text | Groq Whisper (бесплатно) |
| Embeddings | sentence-transformers (multilingual-e5-small) |
| Экспорт заказов | openpyxl |

## Быстрый старт

### 1. Клонирование и настройка

```bash
git clone <your-repo-url>
cd vitaprod_bot

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
.\venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Конфигурация

```bash
# Копируем пример конфигурации
cp .env.example .env
```

Заполните `.env` своими данными (см. `.env.example` для списка переменных).

**Необходимые переменные:**
- `TELEGRAM_BOT_TOKEN` — токен бота от @BotFather
- `MANAGER_TELEGRAM_IDS` — ID менеджеров через запятую
- `GIGACHAT_CREDENTIALS` — credentials от GigaChat API
- `GROQ_API_KEY` — ключ Groq для голосовых сообщений (бесплатно)
- Параметры подключения к PostgreSQL и Qdrant

### 3. Запуск Docker контейнеров

```bash
docker compose up -d
```

Это запустит:
- **Qdrant** (порт 6333) — векторная база для поиска товаров
- **PostgreSQL** (порт 5433) — хранение диалогов

### 4. Загрузка прайс-листа

```bash
python -m scripts.load_price data/prices/price_vitaprod.pdf
```

### 5. Запуск бота

```bash
python -m src.main
```

## Структура проекта

```
vitaprod_bot/
├── src/
│   ├── bot/                    # Telegram бот
│   │   ├── handlers/
│   │   │   ├── start.py        # /start, /clear, кнопки меню
│   │   │   ├── order.py        # FSM оформления заказов
│   │   │   ├── voice.py        # Голосовые сообщения
│   │   │   └── price_query.py  # Обработка текстовых сообщений
│   │   └── keyboards/
│   │       └── order.py        # Inline-клавиатуры заказов
│   ├── core/
│   │   ├── graph/              # LangGraph (память диалогов)
│   │   ├── orders/             # Система заказов
│   │   ├── rag/                # RAG pipeline
│   │   └── prompts.py          # Системные промпты
│   ├── integrations/
│   │   ├── llm/                # GigaChat интеграция
│   │   └── stt/                # Speech-to-Text
│   ├── data/
│   │   ├── parsers/            # Парсер PDF прайс-листов
│   │   └── embeddings.py       # Сервис эмбеддингов
│   ├── db/
│   │   └── vector.py           # Qdrant клиент
│   └── config.py               # Настройки приложения
├── data/
│   ├── prices/                 # PDF прайс-листы
│   └── orders/                 # Экспортированные заказы
├── scripts/                    # Утилиты
└── docker-compose.yml
```

## Голосовые сообщения 🎤

Бот поддерживает голосовые сообщения с распознаванием речи:

1. Пользователь записывает голосовое сообщение
2. Groq Whisper распознаёт речь → текст
3. Текст обрабатывается через тот же pipeline (RAG + LLM)
4. Бот отвечает как на обычное текстовое сообщение

**Пример:**
```
🎤 Голос: "Хочу заказать клюкву лесную 10 кг и кизил 15 кг"
        ↓
🎤 Распознано: «Хочу заказать клюкву лесную 10 кг и кизил 15 кг»
        ↓
Бот: Отлично! Вот ваш заказ:
• Клюква лесная (замороженная) — 10 кг × 450 ₽ = 4 500 ₽
• Кизил (замороженный) — 15 кг × 380 ₽ = 5 700 ₽
Итого: 25 кг — 10 200 ₽
```

## Флоу оформления заказа

```
Клиент: Хочу заказать чернику 10 кг
    │
    ▼
┌─────────────────────────────────────┐
│  📦 Оформляем заказ?                │
│  • Черника — 10 кг × 420 ₽ = 4200 ₽ │
│  [Да, оформить] [Добавить] [Отмена] │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  📦 Выберите упаковку               │
│  [Коробка] [Мешок] [Любая]          │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  ⚖️ Выберите вес тарного места      │
│  [10 кг] [15 кг] [20 кг] [25 кг]    │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  🚚 Способ получения                │
│  [Доставка] [Самовывоз]             │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  📍 → 📅 → 🕐 → 👤                  │
│  Адрес, дата, время, контакты       │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  ✅ Заявка #ABC123 принята!         │
│  → XLSX отправлен менеджерам        │
└─────────────────────────────────────┘
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и главное меню |
| `/clear` | Очистить историю диалога |
| `/help` | Справка по использованию |

## Кнопки меню

| Кнопка | Действие |
|--------|----------|
| 📋 Весь ассортимент | Показать все товары по категориям |
| 📞 Связаться с менеджером | Контакты компании |

## Получение API ключей

### Telegram Bot Token
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте токен в `.env`

### Manager Telegram ID
1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ID в `MANAGER_TELEGRAM_IDS`

### GigaChat Credentials
1. Зарегистрируйтесь на [developers.sber.ru](https://developers.sber.ru)
2. Создайте проект и получите credentials
3. Скопируйте в `.env`

### Groq API Key (для голосовых)
1. Зарегистрируйтесь на [console.groq.com](https://console.groq.com)
2. Создайте API ключ (бесплатно!)
3. Скопируйте в `GROQ_API_KEY`

## Полезные команды

```bash
# Проверить данные в Qdrant
python -m scripts.check_qdrant

# Перезагрузить прайс-лист
python -m scripts.load_price data/prices/price_vitaprod.pdf

# Посмотреть логи контейнеров
docker compose logs -f

# Перезапустить контейнеры
docker compose restart
```

## Troubleshooting

### Бот не отвечает
1. Проверьте что Docker контейнеры запущены: `docker compose ps`
2. Проверьте логи бота
3. Убедитесь что `.env` заполнен корректно

### Ошибка "relation does not exist"
Отправьте любое сообщение боту — таблицы создадутся автоматически.

### Голосовые сообщения не работают
1. Проверьте `GROQ_API_KEY` в `.env`
2. Получите бесплатный ключ на [console.groq.com](https://console.groq.com)

### Ошибка asyncio на Windows
Добавьте в начало `main.py`:
```python
import sys
import asyncio
if sys.platform == 'win32':
    asyncio.set_event_loop_policy(asyncio.WindowsSelectorEventLoopPolicy())
```

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл в репозиторий
- Добавьте `.env` в `.gitignore`
- Используйте разные credentials для dev/prod окружений
- Регулярно ротируйте API ключи

## Лицензия

MIT License